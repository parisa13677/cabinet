.wrapper_with_padding
  #docs.clearfix
    - unless @docs.blank?
      %a{ href: (url_for [doc])}
        .doc
          %p.title= doc.title
          %p.date= time_ago_in_words(doc.created_at)
          %p.content= truncate(doc.content, length:50)
    - else
      %h2 Create Doc!
      %p Statrt Creating Documents and Organizaing your life!
      %button= link_to "Create Doc", new_doc_path
